# Testimonial Capture — {{CLIENT}} (Agent 11)

> ⏸ **BLOCKED until a positive 7-day result exists** (`results/{{client}}.md`). A testimonial without a
> real number is not a testimonial. Copy to `testimonials/{{client}}.md` once the delta is in.

**Goal:** turn the result into a usable proof asset — a result-led quote **with a number**, explicit
permission, and (ideally) a 30-second video.

## Draft (result-led, 2 sentences — for them to approve, in their voice)
> "In the first week, `{{CLIENT}}` recovered `[[ __ ]]` appointments we were missing after hours —
> about `[[ $__ ]]`. `[[ It just answers every call now / I stopped worrying about the phone ]]`."
> — `[[ Owner name, OD ]]`, `{{CLIENT}}`

## Asks (pick the lightest yes first)
- [ ] **Written OK** to use the quote + number publicly (email reply is fine).
- [ ] **30-sec video** (optional, highest value): "What was the phone like before, and what changed in
      week one?"
- [ ] Logo / practice name usage permission.

## Permission record
| Field | Value |
|---|---|
| Quote approved (Y/N) | `[[ ]]` |
| Number included | `[[ ]]` |
| Usage scope (web/ads/deck) | `[[ ]]` |
| Video received (Y/N) | `[[ ]]` |
| Date + who approved | `[[ ]]` |

*Optometry handles PHI but a missed-call/booking-recovery stat contains none — still, only publish
what the owner explicitly approves.*

---
*VERIFY: contains a **number** · has **explicit permission** on record · result-led (not adjectives).
Feeds `ads.md` (the 2-testimonial gate) and `conversions/`.*
