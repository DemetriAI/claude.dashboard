# Motion / Book the Call — optometry (Agent 9) — THE MONEY EVENT

Everything upstream is fuel. This is the conversion to a **booked live 15-minute call this week**.
Live is the edge — never let a reply cool into "I'll follow up."

**This week's booked count: 0** *(awaiting first reply — the outreach in `outreach_cold.md` hasn't been
sent yet on a real account).* Each booking updates `pipeline.csv` to `stage=call_booked` and lands in
the log below.

> Google Calendar + Gmail tools are connected. On a real reply, say the word and I'll **create the
> calendar event, attach the deck/Loom as pre-read, and set the reminders live** — I just don't
> fabricate a booking before a prospect actually replies.

## The motion (run on any reply or positive signal)
1. **Strike same-week, slots only.** Don't ask "when works?" — offer two concrete same-week times:
   *"I've got Thursday 8:15am or Friday 4:30pm — which is easier?"* (Urgency = your edge.)
2. **Confirm with a one-line value reminder.** *"Locked Thu 8:15. 15 minutes — I'll show exactly where
   the missed-call leak is and how we build the fix free in 7 days."*
3. **Send the pre-read.** Attach the practice's Gamma deck (`decks/`) + their Loom (`looms/`) on the
   calendar invite. They walk in pre-sold.
4. **Reminder cadence:** **T-24h** (text + email: value reminder + pre-read link) and **T-1h** (short
   text: *"We still on for 8:15? I've got your numbers pulled up."*).
5. **Hand off to the close.** The call itself runs on the `calls/` discovery system — generate the prep
   doc: `./calls/_new-prep.sh "<Practice>" healthcare` and fill it from the pain brief.

## Booking log
| Practice | Call time | Pre-read sent (deck+Loom) | Reminders set (T-24h/T-1h) | Prep doc | Stage |
|---|---|---|---|---|---|
| *(first booking lands here)* | — | — | — | — | — |

## No-show / reschedule
If T-1h gets silence: one warm text, then auto-offer two new same-week slots. Never drop to "let me
know" — always re-anchor to a specific time.

---
*VERIFY: every booked call has a **time**, a **pre-read attached**, and **reminders set** before it
counts. Print this week's booked count (currently 0). On convert → the call feeds `calls/` then
Agent 10 (`results/`).*
