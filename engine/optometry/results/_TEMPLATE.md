# Results & Proof — {{CLIENT}} (Agent 10)

> ⏸ **BLOCKED until a live pilot exists.** No practice has gone live, so there is **no baseline and no
> day-7 delta to report** — and I will not project or invent one (`SPEED_GATE` honesty: the day-7
> figure must be **real**). Copy this file to `results/{{client}}.md` at go-live and fill it.

**Rule:** capture the **baseline BEFORE go-live**, then the **measurable delta within 7 days**. Real
numbers only — flag anything missing rather than estimating.

## Day 0 — baseline (record BEFORE the AI answers a single call)
| Metric | Day-0 baseline | How captured |
|---|---|---|
| Inbound calls / week | `[[ ]]` | phone system / call log |
| % unanswered (missed/voicemail) | `[[ ]]` | call log |
| Bookings / week | `[[ ]]` | their scheduler |
| No-show rate | `[[ ]]` | scheduler |
| Google reviews (count + rating) | `[[ ]]` | Google Business Profile |

## Instrument the workflow
- [ ] AI front desk live on their number (date: `[[ ]]`)
- [ ] Calls + bookings tracked (recovered calls tagged distinctly from existing-patient calls)
- [ ] Calendar write-through confirmed (AI bookings land in their scheduler)
- [ ] Review-request automation on (if in scope)

## Day 7 — the delta (real figures only)
| Metric | Day 0 | Day 7 | Δ |
|---|---|---|---|
| Calls answered | `[[ ]]` | `[[ ]]` | `[[ ]]` |
| **Recovered missed-call bookings** | 0 | `[[ ]]` | `[[ ]]` |
| No-show rate | `[[ ]]` | `[[ ]]` | `[[ ]]` |
| New reviews | `[[ ]]` | `[[ ]]` | `[[ ]]` |

**Headline number:** `[[ ___ recovered bookings × $___ avg visit = $___ recovered in 7 days ]]`
(or hours saved). This single figure powers Agents 11 (testimonial) and 12 (convert).

---
*VERIFY: baseline captured **before** go-live · day-7 figure is **real**, not projected · missing
data is flagged, not filled with guesses.*
