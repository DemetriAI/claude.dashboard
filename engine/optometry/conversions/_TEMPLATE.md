# Pilot → Convert — {{CLIENT}} (Agent 12) — THE SECOND MONEY EVENT

> ⏸ **BLOCKED until a client is at day 7–12 of a pilot with a proven result.** Copy to
> `conversions/{{client}}.md` then. Terms must have been **pre-agreed before the pilot started**
> (signed agreement, price locked) — billing simply triggers on continue.

**Goal:** convert the free pilot → paid (setup fee + MRR). No renegotiation — the price was locked at
signature; the result just makes "yes" the obvious default.

## The convert conversation
1. **Show the result + the $ it created.** Straight from `results/{{client}}.md`:
   *"Week one recovered `[[ __ ]]` bookings — about `[[ $__ ]]`. That's with the basic version."*
2. **Restate the pre-signed terms** (must match what they signed before the pilot):
   - Setup fee: `[[ $____ ]]`  · Monthly: `[[ $____/mo ]]`
3. **Confirm continue → trigger billing.** *"It's already working on your phones — we just flip it to
   production and the agreement you signed kicks in. Want me to start the first month?"*
4. **Schedule onboarding-to-production** (full build, deeper scheduling rules, review automation, etc.).

## Status
| Field | Value |
|---|---|
| Result $ shown | `[[ ]]` |
| Setup fee (matches pre-pilot signature) | `[[ $____ ]]` |
| MRR (matches pre-pilot signature) | `[[ $____/mo ]]` |
| Continue confirmed (Y/N) | `[[ ]]` |
| Billing triggered (Y/N + date) | `[[ ]]` |
| Production onboarding scheduled | `[[ date ]]` |

**MRR added this convert: `[[ $____/mo ]]`** → roll into the dashboard total in `../README.md`.

---
*VERIFY: result $ shown · terms **match the pre-pilot agreement** · billing confirmed · MRR printed.
For SMB there's no procurement gate (that's the hedge-fund path); just sign → prove → bill.*
